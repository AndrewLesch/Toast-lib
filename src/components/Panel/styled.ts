import styled, { StyledComponent } from 'styled-components';

const PanelContainer: StyledComponent<'div', {}> = styled.div`
  width: 1000px;
  height: 500px;
  margin: 0 auto;
  background-color: black;
  border-radius: 10px;
`;

export default PanelContainer;
